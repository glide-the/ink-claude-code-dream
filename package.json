{
  "name": "@ink-memory/runtime-envelope",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "description": "Fail-closed, transparent launcher and capability contract for Dream's pinned Claude Code runtime",
  "engines": {
    "node": ">=22.0.0 <25.0.0",
    "bun": ">=1.2.20 <2.0.0"
  },
  "packageManager": "bun@1.2.20",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "build": "bun run scripts/build.ts",
    "test": "bun run build && node --test tests/*.test.mjs",
    "test:upstream-sdk": "bun run build && node scripts/run-upstream-sdk-contract.mjs",
    "test:acceptance": "bun run build && node scripts/run-acceptance.mjs",
    "test:official-difference": "bun run build && node scripts/benchmark-official.mjs",
    "test:mcp-matrix": "node scripts/run-mcp-matrix.mjs",
    "test:reproducible": "node scripts/check-reproducible.mjs",
    "verify": "bun run lint && bun run test && bun run test:upstream-sdk && bun run test:acceptance && node scripts/verify-release.mjs && bun run test:reproducible",
    "package": "bun run release:pack",
    "release:pack": "bun run build && node scripts/verify-release.mjs && node scripts/pack-release.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.9",
    "tar-stream": "3.1.7"
  }
}
