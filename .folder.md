<!-- [Input] One original-module implementation, byte-exact reference, compatibility transforms and package tooling. -->
<!-- [Output] Root ownership and actual build-path contract. -->
<!-- [Pos] Runtime repository folder contract. -->
<!-- [Sync] 2026-09-13: replace the incorrect side-snapshot plus parallel Runtime architecture. -->

# Runtime root

`src` is the unique original implementation, not an Ink-designed wrapper:
1,902 files and 35 original module directories. Source provenance is metadata only.
Default build enters `src/entrypoints/cli.tsx`; no `src/cleanroom` remains.

| Path | Owner / role |
| --- | --- |
| src/ | Actual recovered modules; original names, bytes and permissions preserved |
| runtime/source-provenance.json | Original source commit, file/mode/content and directory inventory; no duplicate tree |
| compat/mcp-auth/ | Source-bound MCP behavior deltas; not a second state machine |
| compat/dream-runtime/ | Source-bound Notion child isolation and explicit model projection; original src bytes unchanged |
| runtime/ | Current headless build/profile, source-layout, package and legal gates |
| runtime/history/0.1.7/ | Retired parallel-implementation policies; never active build inputs |
| package/ | Repository-authored package-root selector source |
| scripts/ | Build, verify, fixture, packaging and qualification orchestration |
| tests/ | Original-module/source-build contracts and provider-free tests |
| docs/ | Current design/build/test guides and clearly marked historical receipts |
| dist/ | Current core/package outputs are Git-ignored; older tracked release receipts remain unchanged and are not active implementations |

Project/selector/target expectations are 0.1.9; Dream adoption is pending publication,
SDK 0.2.145 and CLI compatibility 2.1.241 are paired without API changes.
Source copyright is retained; local compilation does not open release gates.
Previous implementation can be recovered from Git commit 38fdd3c.
