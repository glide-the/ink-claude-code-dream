<!-- [Input] Original source subtree, immutable provenance and canonical repository src. -->
<!-- [Output] Ownership of the original reference and its identical actual implementation. -->
<!-- [Pos] Reference folder contract, outside the byte-exact module subtree. -->
<!-- [Sync] 2026-09-13: same original modules now compile from canonical src. -->

# restored-src

`src/` is the byte-exact original reference, matching repository `src/` in every
file, mode and directory. `source-snapshot.json` binds the original commit/subtree/bytes;
`README.md` explains current build ownership and the receipt retains research provenance.
No folder docs or authored headers are injected into either original module tree.
Actual implementation is repository src; this folder does not introduce a second Runtime.
