<!-- [Input] User-authorized, privacy-redacted external qualification results bound to exact clean-room artifact digests. -->
<!-- [Output] Checked JSON attestations whose SHA-256 values open explicit Runtime publication gates. -->
<!-- [Pos] Public review boundary for release qualifications; raw business logs, accounts, tokens, and callbacks never enter Git or npm packages. -->
<!-- [Sync] 2026-08-28: add the Runtime 0.1.3 opaque-model capability acceptance bound to the exact tested source tree and executable. -->
<!-- [Sync] 2026-08-30: bind Runtime 0.1.4 Notion acceptance and the user's explicit clean-room npm release authorization. -->

# Runtime attestations

`dream-real-business-acceptance-0.1.0.json` records the privacy-redacted result of the final Dream business journey. It binds the accepted local fixture tarballs and native executable, documents the public production entrypoints exercised, and records the explicit release authorization.

`dream-real-business-acceptance-0.1.1.json` binds the MCP authentication-routing release to the reviewed Runtime PR/CI evidence, the authenticated and anonymous Server journeys, automatic OAuth callback, tools/resources/prompts inventory, partial-failure isolation, cancel, and same-thread resume. Its artifact digests are fixed before the publication workflow runs.

`dream-real-business-acceptance-0.1.2.json` binds the Messages request-parameter recovery to two successful turns in one normal Dream thread. Its content-free Gateway projection confirms `max_tokens=32000`, configured `effort=low`, `stream=true`, redacted authorization, persisted success, and Admin visibility while excluding request bodies, credentials, account identifiers, and transcripts.

`dream-real-business-acceptance-0.1.3.json` binds the opaque-model output-capability repair to the exact clean-room source tree and darwin-arm64 executable exercised by a normal two-turn Dream Chat/resume journey. The authenticated Admin catalog declared `max_output_tokens=384000`; both settled Gateway requests retained that exact value with `effort=low`, `stream=true`, and redacted authorization. Version 2 of the receipt binds what actually executed and avoids a circular dependency on post-acceptance npm tarballs.

`dream-real-business-acceptance-0.1.4.json` binds the Notion CLI production-Bash repair to the exact clean-room source tree and darwin-arm64 executable exercised by a normal three-turn Dream Chat journey. Fresh and resumed Agent Bash both observed the actor/thread token and file-auth mode, correctly kept the absent workers configuration unset, and completed read-only `ntn` version/doctor/identity checks; the final no-tool Chat turn passed. It also records the user's explicit public clean-room npm authorization while retaining the platform-before-selector order and denying PyPI publication. The receipt contains no account identity, environment value, Notion body, or transcript.

Packaging and workflows verify the selected checked file's SHA-256 through `runtime/cleanroom-artifact-policy.json`. Generated npm packages carry only that digest in `npm-publication-attestation.json`; they do not bundle this receipt or any real account, database row, request body, OAuth credential, callback, transcript, or raw log. Same-SHA CI qualification and registry reinstall remain separate required evidence.
