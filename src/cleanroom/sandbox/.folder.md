<!-- [Input] A canonical Workspace root, bounded command request, and cancellation signal. -->
<!-- [Output] Sandbox adapter contract plus fail-closed and provider-free lifecycle implementations. -->
<!-- [Pos] Clean-room process isolation seam used by the Bash tool. -->
<!-- [Sync] 2026-08-24: add default-deny sandbox and an explicitly test-only bounded subprocess fixture. -->

# Clean-room sandbox

- `sandbox.ts`: defines the trusted sandbox adapter contract. `FailClosedSandbox` is the production default when no isolation backend is installed.
- `process-fixture.ts`: provider-free subprocess fixture for timeout/cancel/cwd contract tests. It is explicitly marked untrusted and is rejected by the tool dispatcher unless a test opts in.

The bounded subprocess fixture is not a filesystem or network sandbox. A production Bash capability needs an audited adapter, such as a separately pinned public sandbox runtime, before it can be enabled.
