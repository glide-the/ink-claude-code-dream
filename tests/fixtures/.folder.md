<!-- [Input] Test-only behavior environment and forwarded CLI process boundary. -->
<!-- [Output] Describe the fake Claude executable and release exclusion. -->
<!-- [Pos] Deterministic provider-free fixture directory. -->

# Fixtures

`fake-claude.mjs` records test-only argv/stdin/cwd/selected env, emits deterministic version/output/exit behavior, and can create a process-group descendant. It is never copied into a release.
